build-job:
  stage: build
  tags:
    - gitlab-org-docker
  script:
    - apt update
    - npm run build

deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
